[build]
builder = "NIXPACKS"

[deploy]
startCommand = "celery -A app.worker.tasks.celery_app worker --loglevel=info --concurrency=1 & python -m http.server $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"